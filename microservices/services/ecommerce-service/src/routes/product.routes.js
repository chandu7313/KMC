import express from 'express';
import { authenticate, authorize } from '@kissan/shared';
import * as pCtrl from '../controllers/product.controller.js';
const router = express.Router();
router.get('/list', pCtrl.listProducts);
router.post('/single', pCtrl.getProduct);
router.get('/:id', pCtrl.getProduct);
router.post('/add', authenticate, authorize(['products:manage']), pCtrl.addProduct);
router.post('/remove', authenticate, authorize(['products:manage']), pCtrl.removeProduct);
router.delete('/:id', authenticate, authorize(['products:manage']), pCtrl.removeProduct);
export default router;
